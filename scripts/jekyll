#!/bin/sh

case $1 in
    serve)
        CONFIG="serve --host 0.0.0.0 --config _config.yml,_config_dev.yml"
        ;;
    *)
        CONFIG="--help"
        ;;
esac

docker run --rm -ti -v "$PWD:/srv/jekyll" -p 4000:4000 --name blog jekyll/jekyll jekyll $CONFIG
